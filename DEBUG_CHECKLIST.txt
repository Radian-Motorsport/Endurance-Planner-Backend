DATA FLOW DEBUGGING CHECKLIST
===============================

This file provides a step-by-step checklist to debug the data flow
from Page 1 selections through Page 2 calculations.

STEP 1: PAGE 1 DATA COLLECTION
-------------------------------
Check these in browser console when clicking "Calculate" button:

□ this.selectedTrack exists and has data
□ this.selectedCar exists and has data  
□ this.selectedDrivers array has items
□ this.selectedSessionDetails exists
□ collectPage1Data() returns complete eventData object

Debug commands to run in console:
window.radianPlanner.selectedTrack
window.radianPlanner.selectedCar
window.radianPlanner.selectedDrivers
window.radianPlanner.selectedSessionDetails

STEP 2: PAGE 2 FORM INPUT COLLECTION
-------------------------------------
Check these values are being read from form:

□ race-duration-hours has a value
□ race-duration-minutes has a value
□ avg-lap-time-minutes has a value
□ avg-lap-time-seconds has a value
□ fuel-per-lap-display-input has a value
□ tank-capacity-display-input has a value
□ pit-stop-time has a value

Debug commands to run in console:
document.getElementById('race-duration-hours').value
document.getElementById('race-duration-minutes').value
document.getElementById('avg-lap-time-minutes').value
document.getElementById('avg-lap-time-seconds').value
document.getElementById('fuel-per-lap-display-input').value
document.getElementById('tank-capacity-display-input').value
document.getElementById('pit-stop-time').value

STEP 3: STRATEGY CALCULATOR INPUT VALIDATION
---------------------------------------------
Check these in console during calculation:

□ extractInputs() returns object with all required fields
□ validateInputs() returns true (currently bypassed)
□ selectedDrivers array is populated
□ raceDurationSeconds > 0
□ avgLapTimeInSeconds > 0

STEP 4: CALCULATION EXECUTION
------------------------------
Check these calculations complete:

□ performCalculations() executes without errors
□ updateDisplays() populates HTML elements
□ populateStintTable() creates stint rows
□ showResultsSections() removes 'hidden' class from sections

STEP 5: RESULTS DISPLAY VERIFICATION
-------------------------------------
Check these sections become visible:

□ overall-summary section visible and populated
□ stint-breakdown-display section visible with table rows
□ sliders-container section visible
□ share-link-container section visible

CURRENT ISSUES IDENTIFIED:
---------------------------
❌ Page 1 data collection returning null/empty values
❌ selectedDrivers array is empty
❌ Track/car garage61_id values are null
❌ Session details incomplete

PRIORITY DEBUGGING ORDER:
--------------------------
1. Fix Page 1 data collection (track, car, drivers selection)
2. Verify Page 2 form inputs are being read correctly
3. Test calculations with hardcoded values if needed
4. Ensure results display properly in HTML sections